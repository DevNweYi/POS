<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@color/colorPrimary"
    android:gravity="center">

    <TextView android:text="Enter your Mobile Number"
        style="@style/StartMainTitle"/>

    <TextView android:text="Please enter mobile number to start application!"
        style="@style/StartSecondTitle"
        android:layout_marginBottom="50dp"/>

    <LinearLayout android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp"
        android:background="@drawable/bd_gray_10r_white">

        <com.google.android.material.textfield.TextInputLayout android:id="@+id/input_mobile_number"
            style="@style/TextInputLayout">
            <EditText android:id="@+id/etMobileNumber"
                style="@style/EditText"
                android:inputType="number"
                android:hint="@string/enter_phone_number"/>
        </com.google.android.material.textfield.TextInputLayout>

        <Button android:id="@+id/btnNext"
            android:text="@string/next"
            style="@style/StartMainButton"/>

    </LinearLayout>

</LinearLayout>
